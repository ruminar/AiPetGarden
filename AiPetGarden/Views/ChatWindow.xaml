<Window x:Class="AiPetGarden.Views.ChatWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="チャット" Width="560" Height="680" MinWidth="420" MinHeight="460"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,12">
            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                <Button Content="Project" Padding="12,5" Click="ProjectButton_Click" />
                <Button Content="設定" Margin="8,0,0,0" Padding="12,5" Click="SettingsButton_Click" />
            </StackPanel>
            <StackPanel>
                <TextBlock x:Name="PetNameTextBlock" FontSize="20" FontWeight="SemiBold" />
                <TextBlock x:Name="BackendTextBlock" Foreground="DimGray" Margin="0,3,0,0" />
            </StackPanel>
        </DockPanel>

        <ListBox x:Name="MessageList" Grid.Row="1" BorderThickness="1" Padding="8"
                 HorizontalContentAlignment="Stretch" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border Margin="0,0,0,10" Padding="9" Background="#FFF3F3F3" CornerRadius="5">
                        <StackPanel>
                            <TextBlock Text="{Binding Sender}" FontWeight="SemiBold" />
                            <TextBlock Text="{Binding Text}" Margin="0,4,0,0" TextWrapping="Wrap" />
                            <TextBlock Text="{Binding TimeText}" Margin="0,4,0,0" FontSize="10"
                                       Foreground="Gray" HorizontalAlignment="Right" />
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <TextBox x:Name="InputTextBox" Grid.Row="2" Margin="0,12,0,0" MinHeight="72" MaxHeight="160"
                 AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"
                 PreviewKeyDown="InputTextBox_PreviewKeyDown" />

        <DockPanel Grid.Row="3" Margin="0,10,0,0">
            <Button x:Name="SendButton" DockPanel.Dock="Right" Content="送信" Width="90" Height="32"
                    Click="SendButton_Click" />
            <TextBlock x:Name="StatusTextBlock" VerticalAlignment="Center" Foreground="DimGray"
                       Text="待機中（会話履歴はまだ実行中のみ保持）" />
        </DockPanel>
    </Grid>
</Window>
